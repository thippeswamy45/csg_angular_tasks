import { DateMmddyyyyazPipe } from './date-mmddyyyyaz.pipe';

describe('DateMmddyyyyazPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMmddyyyyazPipe();
    expect(pipe).toBeTruthy();
  });
});
